import XCTest
@testable import Nifty_Dice_Roller

final class Nifty_Dice_RollerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Nifty_Dice_Roller().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
